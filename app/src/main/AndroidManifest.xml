<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wadektech.eventshub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".ui.FriendsEventsActivity" />
        <activity android:name=".ui.ConcertsAndTheatresActivity" />
        <activity android:name=".ui.SocialEventsActivity" />
        <activity android:name=".ui.ProfessionaEventsActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".auth.ResetPasswordActivity" />
        <activity android:name=".auth.SignUpActivity" />
        <activity android:name=".auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MoreInfo" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>